/**
 * A simple configuration file that will contain information about 
 * the server component and the email address
 */
export const config: { serverAddress: string, userEmail: string } = {
    serverAddress: "http://localhost:8080",
    userEmail: "wenyi0369@gmail.com"
};